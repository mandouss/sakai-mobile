<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noellesun.sakai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- this is the start activity when start up -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Assignment"
            android:label="Assignment" />

        <activity
            android:name=".Resources"
            android:label ="Resources"/>

        <activity
            android:name=".Gradebook"
            android:label="Gradebook"/>
        <activity
            android:name=".Announcement"
            android:label="Announcement" />

        <activity android:name=".eachResource"/>
        <activity
            android:name=".eachSite" />
        <activity
            android:name=".sites"
            android:label="Sites" />
        <activity android:name=".eachAssign" />
        <activity android:name=".eachAnnounce" />

        <activity
            android:name=".Profile"
            android:label="Profile"/>
        <activity android:name=".PdfViewer"></activity>
    </application>
</manifest>